## What changed and why



## How you tested it

- [ ] Rebuilt the devcontainer (if this touches `.devcontainer/`)
- [ ] `colcon build --symlink-install` succeeds (if this touches `ros_ws/`)
- [ ] Ran the relevant sim/hardware flow, not just a build check

## Anything a reviewer should know that isn't obvious from the diff

